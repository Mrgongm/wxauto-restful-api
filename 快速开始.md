# 初始化：

```powershell
# 创建虚拟环境
python -m venv myenv
# 进入虚拟环境
.\myenv\Scripts\activate
# 升级 pip
python -m pip install --upgrade pip
# 安装依赖
pip install -r requirements.txt
```

# 打包：

拷贝app.spec.example至app.spec文件，放入打包目录，并修改信息
编辑器打开该文件，修改第8、9行信息，保存

```
pyinstaller app.spec
```